// This library is CSS/SCSS-only.
// In your app's styles.scss add:
//   @use 'tokens/global';  (CSS custom properties applied to :root)
//
// In component SCSS files use CSS custom properties directly:
//   color: var(--text-primary);
//   height: var(--ctrl-h-md);
//
// For SCSS mixins:
//   @use '@ui/tokens/mixins' as t;
//   @include t.focus-ring();

export {};
